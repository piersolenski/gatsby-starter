import styled from 'styled-components';
import lock from 'utils/lock';

const Section = styled.section`
  ${lock('padding-top', '50px', '100px')};
  ${lock('padding-bottom', '50px', '100px')};
`;

export default Section;
